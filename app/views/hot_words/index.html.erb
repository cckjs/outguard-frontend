<p id="notice"><%= notice %></p>

<table class="table table-hover table-vcenter">
  <tbody>
      <tr>
  <% @hot_words.each.with_index do |word, index| %>
        <td><%= link_to word.content, word.url %></td>
    <% if (index +1) % 5 ==0 %>
        </tr>
        <tr>
    <% end %>
  <% end %>
    </tr>
  </tbody>
</table>
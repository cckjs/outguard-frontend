<table class="table table-hover">
  <thead>
    <tr>
      <th>文章标题</th>
      <th>关键字</th>
      <th>发布时间</th>
    </tr>
  </thead>
  <tbody>
  <% @results["search_content"].each do |doc| %>
    <tr>
      <td><%= link_to doc["title"], doc["url"],target: "_blank" %></td>
      <td><%= doc["keyword"] %></td>
      <td><%= doc["crawl_at"] %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<%= link_to "返回", bd_documents_path, class: 'btn btn-primary' %>
